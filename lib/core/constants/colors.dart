import 'package:flutter/material.dart';

const accentColor = Color(0xFF65B947);
const primaryColor = Color(0xFFE8C37D);
const whiteColor = Colors.white;
const blackColor = Colors.black;
const redColor = Color(0xFFD93A3A);

const accentLightColor = // 10% of accentColor
const blackLightColor = // 10% of blackColor
const dividerLight = // 10% of blackColor
const whiteMuted = // 80% of whiteColor